<div class="min-h-screen bg-slate-950 text-slate-50">
  <div class="border-b border-slate-800 bg-slate-950/80 backdrop-blur">
    <div class="mx-auto flex max-w-4xl items-center justify-between px-4 py-4">
      <div class="flex items-center gap-3">
        <div class="flex h-9 w-9 items-center justify-center rounded-xl bg-emerald-500/10 ring-1 ring-emerald-400/40">
          <span class="text-lg font-semibold text-emerald-300">G</span>
        </div>
        <div>
          <h1 class="text-lg font-semibold tracking-tight">GenChan</h1>
          <p class="text-xs text-slate-400">Posteos anónimos con IA generativa</p>
        </div>
      </div>

      <div class="flex items-center gap-2 text-xs">
        <%= link_to "Volver",
          root_path,
          class: "inline-flex items-center justify-center rounded-full border border-slate-700 bg-slate-950/80 px-3 py-1.5 text-[11px] font-medium text-slate-100 hover:border-emerald-400 hover:text-emerald-300" %>

        <%= button_to "Salir",
          session_path,
          method: :delete,
          class: "inline-flex items-center justify-center rounded-full bg-slate-100 px-3 py-1.5 text-[11px] font-semibold text-slate-950 hover:bg-slate-200" %>
      </div>
    </div>
  </div>

  <main class="mx-auto max-w-3xl px-4 py-10">
    <div class="mb-8 text-center">
      <h2 class="text-2xl font-semibold tracking-tight text-slate-50">
        Crear un nuevo post
      </h2>
      <p class="mt-2 text-sm text-slate-400 max-w-md mx-auto leading-relaxed">
        Compartí una historia, una duda, una idea o lo que tengas ganas.
        Todo se publica anónimamente como
        <span class="font-semibold text-emerald-300">anonymous</span>.
      </p>
    </div>

    <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-6 shadow-lg shadow-slate-950/40">
      <% if @post.errors.any? %>
        <div class="mb-4 rounded-xl border border-red-500/40 bg-red-950/40 p-3 text-xs text-red-200">
          <div class="mb-1 font-semibold tracking-wide">Revisá estos campos:</div>
          <ul class="list-disc space-y-0.5 pl-4">
            <% @post.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @post, class: "space-y-6" do |f| %>

        <div class="space-y-1.5">
          <%= f.label :title,
            "Título",
            class: "block text-xs font-medium uppercase tracking-[0.16em] text-slate-400" %>

          <%= f.text_field :title,
            placeholder: "Escribí un título llamativo…",
            class: "block w-full rounded-xl border border-slate-700 bg-slate-950/80 px-3 py-2.5 text-sm text-slate-50 placeholder:text-slate-500 focus:border-emerald-400 focus:outline-none focus:ring-2 focus:ring-emerald-500/30" %>
        </div>

        <div class="space-y-1.5">
          <%= f.label :body,
            "Contenido",
            class: "block text-xs font-medium uppercase tracking-[0.16em] text-slate-400" %>

          <%= f.text_area :body,
            rows: 8,
            placeholder: "Contá tu historia, idea, confesión o lo que quieras…",
            class: "block w-full resize-none rounded-xl border border-slate-700 bg-slate-950/80 px-3 py-3 text-sm text-slate-50 placeholder:text-slate-500 focus:border-emerald-400 focus:outline-none focus:ring-2 focus:ring-emerald-500/30" %>
        </div>

        <div class="flex items-center justify-between pt-2">
          <%= link_to "Cancelar",
            root_path,
            class: "text-sm text-slate-400 hover:text-slate-200" %>

          <button
            type="submit"
            class="inline-flex items-center justify-center rounded-xl bg-emerald-500 px-5 py-2.5 text-sm font-semibold text-slate-950 shadow-sm shadow-emerald-500/30 transition hover:bg-emerald-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-950"
          >
            Publicar anónimamente
          </button>
        </div>

      <% end %>
    </div>
  </main>
</div>
